# QA Report - 2026-04-17

## Scope
- Backend API smoke test (auth, recipes, recommend, saved, my-recipes, feedback)
- Data QA checks on active `recipes.db`
- Frontend production build smoke
- Duplicate visibility behavior check (`include_duplicates`)

## Environment
- Backend URL: `http://127.0.0.1:8001`
- DB path: `backend-python/recipes.db`
- Total recipes: 5938

## 1) Backend Smoke Test
All steps passed.

- Passed: 16
- Failed: 0
- Slowest endpoint: `POST /recommend` at ~19.189s

Validated endpoints:
- `GET /health`
- `POST /auth/register`
- `POST /auth/login`
- `GET /auth/me`
- `GET /recipes/` (default + diet/cuisine filters)
- `GET /recipes/{id}`
- `POST /recommend/`
- `POST /saved/{id}` + `GET /saved/`
- `POST /my-recipes/{id}/interested`
- `POST /my-recipes/{id}/start`
- `PATCH /my-recipes/{id}/progress`
- `POST /feedback/`

## 2) Data QA (Current Schema)
Current recipe-table schema includes:
- Core: `title`, `ingredients`, `steps`, `cuisine`, `tags`, `total_time`, `difficulty`
- Variant metadata: `base_recipe`, `variant_type`, `cooking_method`, `protein_type`, `difficulty_variance`

Distribution highlights:
- `difficulty`: medium 2164, easy 2103, hard 1671
- `cuisine`: indian 5938
- `variant_type`: standard 5878, quick 24, traditional 24, easy 12
- `protein_type`: vegetarian 5144, non_veg 794

Top duplicate titles/base groups observed: max frequency 2 for sampled top entries.

Heuristic issue counters (rule-based, for manual review):
- `veg_tags_with_meat_words`: 278
- `nonveg_tags_without_meat_words`: 89
- `quick_keyword_but_long_total_time`: 536
- `slow_keyword_but_short_total_time`: 23

A random manual-review sample of 120 recipes was generated during QA.

## 3) Frontend Build Smoke
Build status: success.

Command:
- `npm.cmd run build`

Notes:
- Build completed successfully with Vite.
- Large chunk warnings present (performance optimization opportunity, not a blocking failure).

## 4) Dedupe Behavior Check
Compared API listing with and without duplicate inclusion for limit=400:

- Default (`include_duplicates` omitted):
  - total rows: 400
  - duplicate extra rows by title: 0
  - titles with duplicates: 0

- With `include_duplicates=true`:
  - total rows: 400
  - duplicate extra rows by title: 3
  - titles with duplicates: 3

Result: dedupe behavior is working as expected.

## Conclusion
- System is operational and stable for the validated paths.
- Backend and frontend smoke checks passed.
- Duplicate suppression behavior is correct by default and reversible via query flag.
- Data labeling heuristics surfaced review candidates; these should feed the next QA cleanup pass.
